
<!DOCTYPE html>
<html>
    <head>
        <title>My Webpage</title>
    </head>
    <body>
        <h2>Dodajesz odpowiedzi do pytania {{questionData.content}}</h2>
        <h3>Dodaj Pytanie</h3>
        {{ form_start(formoffered) }}
        {{ form_widget(formoffered) }}
        {{ form_end(formoffered) }}
         {% if haveQue == true %}
            <a href="/question/answers/add/thanks/{{ questionData.getId }}"><button>Powrót</button></a>
        {% endif %}
    </body>
</html>