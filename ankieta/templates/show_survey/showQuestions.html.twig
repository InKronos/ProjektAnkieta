
<!DOCTYPE html>
<html>
    <head>
        <title>My Webpage</title>
    </head>
    <body>
        <h3>Pytania ankiety {{ survey.name }} <h3>
        <ul id="list">
            {% for item in items %}
                <a href="/show/survey/question/{{ item.id }}"><li>{{ item.content }}</li></a>
            {% endfor %}
        </ul>
        <a href="/add/question/{{ survey.id }}"><button>Dodaj kolejne pytanie</button></a>
        <a href="/show/survey"><button>Powrót</button></a>
    </body>
</html>