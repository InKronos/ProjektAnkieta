
<!DOCTYPE html>
<html>
    <head>
        <title>My Webpage</title>
    </head>
    <body>
        <h3>Dodane odpowiedzi: <h3>
        <ul id="list">
            {% for item in items %}
                <li>{{ item.content }}</li>
            {% endfor %}
        </ul>
        {{ form_start(formend) }}
        {{ form_widget(formend) }}
        {{ form_end(formend) }}
    </body>
</html>